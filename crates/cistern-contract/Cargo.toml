[package]
name = "cistern-contract"
description = "what a quota-cistern surface and the core agree on"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
interprocess = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# Only one core may hold the stores, and only the kernel can say so across processes.
# A lock the kernel gives back when the holder dies is what a lock file cannot be.
[target.'cfg(unix)'.dependencies]
nix = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
tempfile = { workspace = true }

[lints]
workspace = true
